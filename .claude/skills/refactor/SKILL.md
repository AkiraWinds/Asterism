# Refactoring Skill

When refactoring or unifying code:
1. ALWAYS propose the architectural approach first
2. Use ONE shared abstraction, never feature-specific code paths
3. Show the unified interface before implementation
4. Wait for approval before proceeding
